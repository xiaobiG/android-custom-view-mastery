{
  "name": "advanced-android-custom-view-book",
  "version": "2.0.0",
  "private": true,
  "description": "Android 自定义控件进阶：从 View 机制到 Compose 互操作",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "vitepress dev . --host 127.0.0.1",
    "serve": "vitepress dev . --host 127.0.0.1",
    "read": "python scripts/read_book.py",
    "build": "vitepress build .",
    "check": "python scripts/validate_book.py && python scripts/link_check.py",
    "audit": "npm audit --audit-level=critical"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
